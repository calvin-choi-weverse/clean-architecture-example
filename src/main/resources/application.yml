spring:
  datasource:
    hikari:
      driver-class-name: org.h2.Driver
      username: sa
      jdbc-url: jdbc:h2:~/test;AUTO_SERVER=TRUE
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
